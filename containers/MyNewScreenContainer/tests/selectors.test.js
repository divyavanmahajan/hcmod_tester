// import { fromJS } from 'immutable';
// import { selectMyNewScreenContainerDomain } from '../selectors';

describe('selectMyNewScreenContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
